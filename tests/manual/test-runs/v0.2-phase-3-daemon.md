# v0.2 Phase 3 — Scheduled curation daemon walk evidence

| Field | Value |
| --- | --- |
| Host | Mac mini Apple Silicon |
| OS | macOS 25.5.0 (Darwin 25.5.0) |
| Date | 2026-05-30 |
| Branch | `v0.2-phase-3-daemon` (PR #33) |
| Corpus | 5 sources, 14 chunks, 6 wiki_pages (from prior smoke walks) |
| Schedule unit | `com.compendium.curate` (`~/Library/LaunchAgents/com.compendium.curate.plist`) |
| Stubs | `COMPENDIUM_EMBED_STUB=1 COMPENDIUM_SYNTH_STUB=1` (curate runs with stub seams) |

Walk against `tests/manual/smoke_test.md` § Phase 3 (v0.2). Scenarios
v0.2-3.1 → v0.2-3.6.

## v0.2-3.1 — Install at default cadence

```sh
uv run python -m compendium schedule install
```

| Check | Result |
| --- | --- |
| Result | PASS |
| Exit | 0 |
| Plist | `~/Library/LaunchAgents/com.compendium.curate.plist` exists |
| `<integer>3600</integer>` under `StartInterval` | ✓ |
| `launchctl print gui/<uid>/com.compendium.curate` | succeeds |

## v0.2-3.2 — Install at a custom cadence

```sh
uv run python -m compendium schedule uninstall
uv run python -m compendium schedule install --every 30m
```

| Check | Result |
| --- | --- |
| Result | PASS |
| Exit | 0 |
| `<integer>1800</integer>` under `StartInterval` | ✓ |

## v0.2-3.3 — Status of a loaded unit

```sh
uv run python -m compendium schedule status
uv run python -m compendium schedule status --format json
```

Text output:

```
schedule: loaded=True state=not running
  unit_path: /Users/giuseppe/Library/LaunchAgents/com.compendium.curate.plist
  interval:  1800s
  last_fired: (never exited)
  next_fire:  (unknown)
```

JSON output:

```json
{
  "loaded": true,
  "unit_path": "/Users/giuseppe/Library/LaunchAgents/com.compendium.curate.plist",
  "state": "not running",
  "last_fired": "(never exited)",
  "next_fire": null,
  "interval_seconds": 1800
}
```

| Check | Result |
| --- | --- |
| Result | PASS |
| Exit | 0 |
| `loaded=True` | ✓ |
| `interval` matches install (1800s) | ✓ |
| `last_fired="(never exited)"` before any fire | ✓ |
| `next_fire=null` on macOS | ✓ (launchctl does not surface next-fire wall clock) |

## v0.2-3.4 — Manual kick produces a `graph_analysis_runs` row

```sh
# Pre-count
psql -c "SELECT count(*) FROM graph_analysis_runs"  # → 4

# Kick the unit
launchctl kickstart -k gui/$(id -u)/com.compendium.curate

# Wait + re-check
psql -c "SELECT count(*) FROM graph_analysis_runs"  # → 5
```

| Check | Result |
| --- | --- |
| Result | PASS |
| Pre-count | 4 |
| Post-count | 5 (delta +1) |
| Time to new row | < 1 s after kick |
| New row id | `a80ffa3d-679d-443d-b941-b19be62a6d7f` |
| New row `started_at` | `2026-05-30 18:12:57.399502+00:00` |
| New row `completed_at` | `2026-05-30 18:12:57.399502+00:00` |
| New row `signal_count` | `0` (no new signals — corpus already drained by earlier runs) |
| New row `summary` | `{"by_kind": {}, "skipped": []}` |

## v0.2-3.5 — Status after a fire shows `last_fired`

```sh
uv run python -m compendium schedule status
```

Text output after the v0.2-3.4 kick:

```
schedule: loaded=True state=not running
  unit_path: /Users/giuseppe/Library/LaunchAgents/com.compendium.curate.plist
  interval:  1800s
  last_fired: 0
  next_fire:  (unknown)
```

| Check | Result |
| --- | --- |
| Result | PASS |
| Exit | 0 |
| `last_fired` transitions from `(never exited)` to `0` (the most-recent exit code) | ✓ |

(macOS `launchctl print` reports the most recent exit code as
`last exit code`; a successful run shows `0`. The status command
surfaces that value as the `last_fired` field. macOS does not expose
a wall-clock last-fire timestamp via `launchctl print`.)

## v0.2-3.6 — Uninstall + idempotent uninstall

```sh
uv run python -m compendium schedule uninstall  # first call
uv run python -m compendium schedule uninstall  # idempotent re-run
uv run python -m compendium schedule status     # expect exit 1
```

| Check | Result |
| --- | --- |
| Result | PASS |
| First uninstall exit | 0 |
| Plist removed | ✓ |
| Second uninstall exit | 0 (reports "not installed") |
| Status after uninstall exit | 1 (state="absent") |

## Findings

None. All six scenarios passed cleanly on the primary host. The
integration round-trip (`tests/test_schedule.py::test_schedule_install_kick_uninstall`)
already exercises the same install → kick → assert-row → uninstall
sequence under pytest's `-m integration` marker; the manual smoke
walk above mirrors that test using the documented operator commands.

## Acceptance roll-up

All six smoke scenarios PASS:

| # | Scenario | Result |
| --- | --- | --- |
| v0.2-3.1 | Install at default cadence | PASS |
| v0.2-3.2 | Install at a custom cadence | PASS |
| v0.2-3.3 | Status of a loaded unit | PASS |
| v0.2-3.4 | Manual kick produces a `graph_analysis_runs` row | PASS |
| v0.2-3.5 | Status after a fire shows `last_fired` | PASS |
| v0.2-3.6 | Uninstall + idempotent uninstall | PASS |

Phase 3 acceptance criteria from `docs/COMPENDIUM_V0.2_BUILD.md` §
Phase 3 met on the primary host with the merged stack.
