# Tasks — arch-index-document-shape

- [x] 1.1 Shape rows + derived builders + field constants in `documents.py`;
  expected-dict (wire-freeze) tests; mapping-agreement test.
- [x] 2.1 `DisplayFields` mixin on `Hit`/`FusedHit`; searchable lists derived;
  pipeline consumes accessors (no raw `.get()` on hit fields).
- [x] 3.1 Docs + smoke section; full fast + golden + ci-smoke green.
