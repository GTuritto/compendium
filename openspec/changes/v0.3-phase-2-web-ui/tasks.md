# Tasks — v0.3-phase-2-web-ui

- [ ] 2a streamlit dependency; `compendium/web/` (the Streamlit app: Ask /
  Search / Pages / Curation over facade + tui/data); `compendium web`
  subcommand (loopback default, execs streamlit).
- [ ] 2b tests: launcher argv construction; AppTest view smokes with a
  monkeypatched facade (no stores).
- [ ] 2c ADR-015 + web-ui.md + MANUAL/CONTEXT/CLAUDE/DECISIONS + smoke
  section; full fast + golden + ci-smoke green.
- [ ] 2d `./release.sh 0.2.5` in the completion commit; after merge, the v0.3
  close-out cuts `0.3.0` + C4 refresh.
